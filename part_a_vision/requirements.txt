# ── Core ML ───────────────────────────────────────────────────────────────────
torch>=2.0.0
torchvision>=0.15.0
transformers>=4.30.0          # SegFormer lives here

# ── Geospatial ────────────────────────────────────────────────────────────────
# GDAL: DO NOT pip install on Windows — use conda or OSGeo4W instead
# Linux: pip install GDAL works IF libgdal-dev is installed
# Windows: conda install -c conda-forge gdal
# Then: pip install rasterio
rasterio>=1.3.0
pyproj>=3.4.0
shapely>=2.0.0
osmnx>=1.3.0
fiona>=1.9.0

# ── Computer Vision / Morphology ──────────────────────────────────────────────
scikit-image>=0.20.0
scipy>=1.10.0
opencv-python>=4.7.0
albumentations>=1.3.0
sknw>=0.14                    # used by Part B; keep in sync with their version

# ── Standard Scientific Stack ─────────────────────────────────────────────────
numpy>=1.24.0
pandas>=1.5.0
matplotlib>=3.7.0
tqdm>=4.65.0

# ── Inference Optimization ────────────────────────────────────────────────────
onnx>=1.14.0
onnxruntime>=1.15.0           # CPU inference

# ── Visualization ─────────────────────────────────────────────────────────────
folium>=0.14.0
streamlit>=1.22.0

# ── Utilities ────────────────────────────────────────────────────────────────
requests>=2.28.0
PyYAML>=6.0